{
  "name": "verdict",
  "version": "0.1.0",
  "description": "Multi-model adversarial security review GitHub Action -- independent Claude + GPT-5.6 Sol reviewers plus Semgrep, never silently merged into a majority vote.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node scripts/run-tests.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/snapshot.js && node --check src/secret-scan.js && node --check src/semgrep.js && node --check src/combine.js && node --check src/report.js && node --check src/verdict.js"
  },
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "js-yaml": "^4.3.2",
    "minimatch": "^10.2.6"
  }
}
